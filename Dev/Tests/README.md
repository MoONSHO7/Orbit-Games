# Orbit-Games offline checks

## Description

Offline Lua 5.1 checks for the game-type registry, generic transport/discovery, Orbit-Quiz save migration, Cards/Texas Hold'em and Quiz.

## Purpose

Exercise deterministic behavior without a World of Warcraft client. Native networking, restricted-state behavior, SavedVariables disk timing, and rendered UI still need two-client in-game verification.

## Implementation

Run `python Dev/Tests/run.py` from the addon folder with Python and `lupa.lua51` available. `run.py` compiles every Lua file, rejects UTF-8 BOMs and every first-party visible-chat sink in production, compatibility and source-preview scripts, checks addon-list branding, runtime paths and source/release exclusions, then loads the real TOC into a fresh mocked runtime for each suite. Runtime mocks make chat-frame writes, global printing and visible sends fail immediately while retaining instrumented native addon messages for transport/discovery checks. The icon, six streak masters and all 60 Ogg playback variants must survive packaging without Orbit installed.

`Rules.lua` covers normalization, canonical encode/decode, detached values, allowed bounds and malformed input. `Scoring.lua` covers frozen legacy arithmetic, authored rewards/penalties, capped streak bonuses, rounding boundaries and bundled-rule long-run guessing balance for four to six choices. `Model.lua` covers author-owned clocks, answer locking, final-selection timing, per-result live-score clamping without hidden debt below zero, streak resets, shuffled/ordered cycles, finite limits and voids, plus schema-6 archive preservation, independent appearance saves and retired chat-setting cleanup. `Packs.lua` checks transactional registration, rule isolation, compatible-only All packs and the first retained validation failure reaching the Quiz Host notice; the runner repeats it for every supported locale.

`CompanionPack.py` reads the example TOC, requires only Orbit-Games, and loads `OrbitGames.Quiz:RegisterPack` in a distinct addon namespace against a production-only core. A registration-only public API catches reliance on private framework members. Run `python Dev/Tests/CompanionPack.py` for this focused check.

`PersonalScores.lua` covers account-wide signed pack/ruleset accounting across hosts, zero-floored public totals, personal schema-1â†’2 migration, untouched v2 archives, v3 streak receipts and separate display rows. It retains top-level schema 1â€“5 upgrade coverage without guessed backfills. Checks exercise transactional rejection, durable duplicate/out-of-order receipts, compressed ranges during continuous play, Unicode host identities, bounded recent records, explicit capacity failures and detached summaries. These checks verify accounting, not anti-cheat security.

`Scores.lua` covers the two live dropdown scopes and their native switch lifecycle. It requires exactly Personal score and Current game, excludes both archive scopes, removes the passive hint lane and visible rule-description blocks, and verifies that the score list reclaims the freed height. Personal rows come only from `PersonalScores:GetScoreRows()`, keep exact rulesets separate, preserve removed-pack titles and distinguish zero, positive and missing totals. Current-game checks preserve authoritative host order while participants see only their own nonnegative total and the pack summary remains compact. Internal recovery fixtures retain every archived row beyond twenty with decimal/current and integer/legacy formats while proving neither archive scope returns to the player menu. Pool reuse, stale-field cleanup, empty messages, scroll resets, constrained title/score columns, unchanged-refresh caching and source immutability are checked independently of setup orchestration.

`Comms.lua` tests the `ORBITGAMES1` codec, fragmentation, queue bounds, retries, mode routing and restricted input. `Runtime.lua` exercises registry dispatch, Quiz host progression, live totals clamped after every delta so a correct result always increases a zero score, visual command routing (`status`/unknown â†’ Games, `packs` â†’ Host, `scores` â†’ Results) and the boss-only automatic-wait boundary, including native-query authority, ordinary-combat exclusion, overlapping chat restrictions and manual-pause ownership. `Session.lua` checks Quiz protocol 2 question/result metadata, signed deltas, nonnegative cumulative totals, immutable identities, exact score validation, receipt acknowledgements and demand-driven standings. Standings fixtures cover host-authoritative order, monotonic replacement, duplicate/stale/conflicting revisions and isolation from scoring and Personal receipts.

`HostAudiences.lua` covers the additive root preference, all seven nonempty Server/Guild/Party combinations, detached reads and writes, legacy all-selected defaults, exact reload persistence and atomic rejection of malformed or empty selections.

`CardsEngine.lua` exercises the pure Texas Hold'em boundary independently of frames and transport: canonical atlas IDs, injected decks, five-to-seven-card ranking, heads-up order, turn deadlines, full and short raises, cumulative reopening, side pots, uncalled refunds, odd chips, automatic runouts and chip conservation. Recipient projections must omit the deck, burns and unrevealed opponent cards. Mocks do not establish host fairness or move WoW gold.

`CardsIntegration.lua` covers Cards schema-1/2/3â†’4 normalization, whole-Gold bounds, two-decimal compact K/M/B/T rounding boundaries, buy-in-relative blind detents, detached settings, bounded SharedMedia font keys, bounded session results, removed legacy payment fields, Results-command UI routing, versioned message grammar, projection round-trips and registration. Real Controller/Session fixtures exercise UTF-8 joins, automatic social seating, active-hand reconnects, monotonic private snapshots, accepted/rejected/retried actions, reply loss, timeouts, disconnected-seat reconciliation, overlapping restriction/encounter waits, manual-pause takeover, public discovery and ungated rebuys. Deprecated wire currency/funding slots remain shape-compatible inside the new Cards protocol but cannot restore old behavior.

`CardsUI.lua` covers the whole-Gold buy-in, the 21-stop small-blind slider and its derived big blind at 1,000, 100,000 and 10,000,000 Gold, plus deterministic compact card/dealer atlas regions, the above-board coin-atlas numeric pot, 25%-reduced coin-atlas player balances with white values, five stable community backs, midpoint-swapped native horizontal flips in both directions with slower turn/river pacing, enlarged board/hole-card geometry, anonymous opponent backs, identity-stable upward fold/disconnect exits and downward next-hand/reconnect/join entries with continuous list/table reflow, snap-free mid-motion reversal, full-capacity row replacement and seatless restriction overlays that preserve the last authoritative list. It also covers dealer handoff, the transparent Quiz-style two-to-eight-player layout, first snapshots with prior folds or disconnects omitted, the transient status lane collapsing when empty and expanding/reflowing player rows when populated, full-row actor backdrops without turn prose, the Host form's stored one-line labels, uniform native typography, aligned full rows, equal paired cells and forty-unit rhythm, the copy-free Settings body and action-dependent shared footer, the compact mode-local font picker, live table-wide SharedMedia binding with native size preservation, recipient privacy, the projection-bounded template-free wager slider's exact endpoints, strictly increasing sampled legal totals, revision reset/preservation, pending state, thin custom art aligned to the action-group span and numeric third-action label, latest-action/status precedence, payout categories, the sole winner's right-pinned left-growing five-face/two-back strip with refresh-stable flips, texture-free text controls, shadows, the lowered Quiz gradient timer with its persistent two-pixel backdrop and four-pixel fill, visible-edge growth, five-card-row-only edit dragging, anonymous edit backs, pixel alignment and pooled result rows without payment controls. Frame mocks verify wiring and geometry; native card rendering and physical interaction remain in-game boundaries.

`Discovery.lua` isolates `ORBITGAMESDISC2`, immutable game/activity versions, capacity and joinability, broad queries, Host-to-filtered advertisements/withdrawals, Server/Guild/Party route mapping, direct probes, bounded queues, expiry, secret payloads and restrictions. `Interface.lua` checks the native Host-to checkbox menu above Game Type, its nonempty selection, persistence and active-session lock alongside the shared Results contract, Games session/Leave footer, clipped identities, notice lane, page exclusivity, mode Host footers, passive-copy removal, no-action footer suppression, ScoreView integration, one-column answers, the permanent current total, confirmed delta overlay, private top-100 tooltip lifecycle and the retained timer backdrop.

`Appearance.lua` checks compact Orbit-style `Scale`/`Font` rows, detached native Edit Mode callbacks, live drag/selection updates, all 31 scale values, screen bounds and normalized positioning. Font checks use the real bundled SharedMedia library and cover previews, search, bounded row reuse, saved selection, fallback/retry, late registration and unchanged question/answer state. Rendering and glyph coverage still require the game.

`WidgetPosition.lua` checks the four left/right and top/bottom growth zones, the inclusive right-side midpoint, true visible corner anchors, fixed growth across question and scale changes, and scaled drag releases without a jump. Only the literal question text may initiate movement; the full content frame, background and answer rows are rejected. The moving root stays fixed through in-drag content reflow; drops clamp/save once and refresh immediately, while resizing alone never persists position. Hide/close/display/scale cleanup retains post-drop click suppression and left-aligned answer ordering. Physical mouse movement remains an in-game verification boundary.

`SoundSettings.lua` checks independent volume persistence/defaults, invalid-value rejection, all eleven slider positions, immediate mute and next-clip nonzero volume changes without cutting current playback, resetting visuals or replaying audio. Every audible level resolves through the same Ogg playback path and no global sound CVar may change. `python -B Dev/SoundVolumes.py --check` separately uses FFmpeg/FFprobe to verify decoding, attenuation, the authored fade/pad duration and safe master/playback endpoints; audible client behavior still needs the game.

`Minimap.lua` loads the real embedded broker/icon libraries, verifies login positioning, idempotent/optional-library initialization, left/right-click commands and the library's private tooltip. Native drag handlers write only the live minimap preferences; schema defaults, angle normalization and atomic validation remain independent of scores, setup, appearance and sound. Source/release runs reload the saved drag settings into a fresh runtime. Native minimap rendering and physical mouse behavior still need the game.

`HUDPolish.lua` exercises text-only press/release and cancellation, typography, shadow padding and the reserved winner popup. It checks the permanent score, confirmed delta overlay, animation timing/replay prevention and lifecycle resets independently of Personal accounting. Large authored totals and deltas exercise preallocated score width across fonts, scales and viewports without moving text on selection or reveal.

`StreakToasts.lua` checks sound mapping, native playback completion independent of visual timing, tail-only polling/cancellation, bounded queue/coalescing and playback failures. Rendering checks cover pixel-aligned 80% burst geometry, its moving shine mask and fixed-size captions at every tier. `HUDStreaks.lua` follows real host closures through five-to-eleven streaks and participant result/name packets into the widget; it covers incremental name resolution, replay gates, untouched Q/A geometry, queue retention across questions, packet waits, pauses, restrictions and natural final-session teardown while explicit lifecycle cancellation still stops playback. Audio mocks verify calls, not audible playback.

`VisualStability.lua` checks that answer submissions, acknowledgements and repeated question packets leave geometry, text layout, allocations and inactive feedback untouched. Press feedback is confined to the pressed label, never its hit area or surrounding layout. Coverage includes physical-grid placement, snapped Host-to/Game Type rows, mode-page clearance, the Games status/action footer, the shared Host footer's idle/active grid, action-dependent divider visibility, divider joins, outlines, scroll rounding, batched layout and cancellation of old scroll motion on resets/new questions. Injected native-range fluctuations test the explicit-content boundary, not an observed C++ rendering sequence.

`Development.lua` verifies the opt-in sample game list, Games-footer ownership, fixture variety, full-list scrolling, row-pool reuse, inert Join callbacks, real-list restoration, silent visual command feedback and isolation from discovery, sessions and saved data. `CardsPreview.lua` drives the real Hold'em model and table through one local plus seven computer seats. Its fixed private random stream must reuse the production shuffle, produce valid distinct successive decks and reproduce after restart without touching global randomness. Fixed-seed distribution thresholds and duplicate table-style assertions are deliberately excluded; CardsUI owns presentation coverage. The real model must consume sixteen unique hole cards, three burns and five board cards while preserving projection privacy; UI coverage retains community backs, dealer/actor ownership, automatic and interactive legal actions, live-game refusal, ticker cleanup and production-state isolation. `ToastPreview.lua` checks the local 5â€“11 sound/animation showcase, live-session refusal, unchanged appearance/data/network state, restart/cancellation and natural queue completion. All three run against source and release-style TOCs; gameplay must work without development commands or strings.

`ScrollingLabels.lua` checks overflow-only native translations, endpoint pauses, full-width text behind a fixed clip, hide/show and scale changes, pooled-row reuse, and uninterrupted motion during unrelated lobby updates. Cards coverage also verifies that physical clip insets preserve its Quiz-style shadows. Mocks verify animation configuration/lifecycle; native clipping and interpolation still need the game.

Template-aware stubs distinguish native button font strings from independent labels and record layout/visibility calls. Native tooltip stubs record both `AddLine` and `AddDoubleLine` on the owning private frame, while private/global tooltip state remains separate. Edit Mode slider rows remain plain Frames; their shared control contract must delegate enabled state to the inner MinimalSlider so its track, thumb and steppers update together. Owned Font objects inherit/copy backing properties; `Font:SetFont` deliberately has no success return while FontString probes do. Synthetic font metrics exercise height/line-limit constraints, not rasterized glyphs or visible shadows. Frame pools reset newly created objects before their first acquisition, matching Blizzard's native lifecycle. Gradient snapshots verify colors/reuse; animation mocks track targets, timing, scale endpoints and final alpha without rendering motion. Replayed animations cannot inherit the previous toast's elapsed step. Sliders retain native callbacks; search boxes retain focus/clear scripts. Startup dispatches registered addon/login handlers, including LibDBIcon. Tests leave the `Orbit` global absent and reject an Orbit TOC dependency.

`Network.py` exchanges real encoded packets between isolated runtimes: discovery, fragmentation, dropped/reordered messages, reconnects, host switches and sixteen participants. It covers variable clocks, locked-answer recovery, shared capped streaks, finite quizzes and final-result recovery. Standings scenarios prove that no board transfers before hover, every current participant can request the same host revision, and duplicate/reordered snapshots neither alter scores nor replay deltas. Five-round full-roster tests check group milestones, compact result payloads and next-question readiness; dropped name maps, incremental resolution and mid-open reconnects cannot lose earned results or replay old toasts. Progress checks retain per-ruleset/host/reload boundaries. These are simulated clients, not live delivery.

`CardsNetwork.py` uses the same real fragmented transport with eight isolated Cards runtimes. It checks full-table delivery without snapshot starvation, recipient privacy, rejected wagers delayed across a street transition and preserved join retry state. `CardsIntegration.lua` also covers duplicate welcomes and join/action deduplication; `CardsEngine.lua` rejects no-limit completion of short opening all-ins.

`Lore.py` audits the bundled catalogue's 1,000â€“2,000-question size, permanent IDs, distinct prompts/choices, four-to-six answer counts, byte limits, all 17 game eras, difficulty/source metadata and source-derived word budgets. Retired comic IDs/publication metadata are rejected; isolated archive loading verifies disjoint IDs while TOC/package checks keep it out of play and releases. Draft assembly reads only active era files. `python Dev/Tests/Lore.py --draft --review` flags similar or reciprocal questions for editorial review. This is structural/provenance validation, not automatic lore fact-checking.

`Lore.lua` runs the entire bundled catalogue through two continuous shuffled cycles, checking remapping, metadata, final-selection timing, retries, scores, bounded state and completeness. It reloads old personal progress alongside content version 2 and the new rules, checking that original scores/duplicate protection survive unchanged while new results enter their ruleset bucket under the same pack identity.

Variable-choice regressions exercise 4/5/6-answer registration, E/F selections, remapping, history compatibility, six text-target reuse, long-content bounds, same-round reconnects, older-protocol rejection and source/answer-key privacy. Sixteen-client maximum-payload simulations cover transport bounds, the twenty-second readiness window and a full authored answering clock after readiness.

## Gotchas

- Supply deterministic random functions for reproducible deck/choice tests; continuous cycles must not grow per-round identity storage.
- Keep the production TOC as the load-order authority. The consolidated application strings must exist before registry and UI modules capture them.
- A mocked addon-message acknowledgement proves code behavior, not Blizzard permission, realm reachability, or native timing.
- Guessing checks assume uninformed uniform final choices; long-run streak checks are not a guarantee about individual rounds or arbitrary author rules. Historical fixture expectations must remain frozen when live scoring changes.
- Frame stubs check control wiring, pooled reuse, edit-only movement, pixel-grid arithmetic and scroll contracts, not rasterized alignment, native font rendering, asynchronous layout or combat behavior.
- Offline locale-code coverage exercises pack behavior; it does not mean all player-facing strings are translated.
- Do not add live-client verification claims based solely on a passing offline run.
- TOC development markers are comments to WoW. Test both raw-source loading and packager-style omission; `.pkgmeta` must also exclude the entire `Dev` folder.

## References

- `../../Modes/Quiz/Rules.lua`, `Model.lua`, `Scoring.lua`, `PackRegistry.lua` and `Data/PersonalScores.lua` â€” Quiz contract, model and progress.
- `../../Network/Identity.lua`, `../../Network/Transport.lua`, `../../Network/Discovery.lua` and `../../App/Runtime.lua` â€” shared routing and orchestration.
- `../../UI/Setup.lua`, `../../Modes/Cards/UI/` and `../../Modes/Quiz/UI/` â€” shared shell and mode presentation.
- `../../Docs/QUICKSTART.md` â€” commands and two-client in-game verification checklist.
